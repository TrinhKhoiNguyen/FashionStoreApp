<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked state -->
    <item android:state_checked="true">
        <layer-list>
            <!-- Border -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/transparent" />
                    <stroke
                        android:width="2dp"
                        android:color="@color/accent_burgundy" />
                    <corners android:radius="4dp" />
                    <size
                        android:width="24dp"
                        android:height="24dp" />
                </shape>
            </item>
            <!-- Check mark -->
            <item
                android:drawable="@drawable/checkbox_tick"
                android:gravity="center" />
        </layer-list>
    </item>
    <!-- Unchecked state -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="2dp"
                android:color="@color/border_light" />
            <corners android:radius="4dp" />
            <size
                android:width="24dp"
                android:height="24dp" />
        </shape>
    </item>
</selector>
